class Driver:
    number_of_drivers = 0
    is_inactive = False

    @staticmethod
    def sort(a):
        return ')))'

    def __init__(self):
        Driver.number_of_drivers+=1
        self.money = 0
        self.ciga = []
        self.keys = ''
        self.decorations = []

'''ds=[]
for i in range(100):
    ds.append(Driver())
print(Driver.number_of_drivers)
print(ds[50].number_of_drivers)
'''

ds= Driver()
print(Driver.f())

Driver.sort()